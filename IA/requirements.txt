# requirements.txt

# Web framework
streamlit>=1.45.1

# Claude API client (Expert principal)
anthropic>=0.51.0  # Mise à jour vers la dernière version

# PDF reading
PyPDF2>=3.0.0

# DOCX reading
python-docx>=1.0.0

# HTML parsing (NOUVEAU - Support fichiers HTML)
beautifulsoup4>=4.12.0

# Image processing
Pillow>=9.0.0

# Pour charger les variables d'environnement localement (optionnel)
python-dotenv>=1.0.0

# Pour convertir Markdown en HTML (pour export)
Markdown>=3.3.0

# --- OPTIONNEL : Décommentez les lignes suivantes si le support Excel (.xlsx) est requis ---
# --- ATTENTION : Cela augmentera considérablement la taille de l'application déployée ! ---
# pandas>=1.5.0
# openpyxl>=3.0.0
